<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/rounded_edittext.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#F2FAF8"/>

    <!-- Rounded corners -->
    <corners
        android:radius="18dp" />
    <!-- Light border -->
    <stroke
        android:width="1dp"
    android:color="@color/blue_100" />

    <!-- Optionally, you can set padding -->
    <!--
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
    -->
</shape>
